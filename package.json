{
  "scripts": {
    "format": "prettier --write ."
  },
  "devDependencies": {
    "pre-commit": "^1.2.2",
    "prettier": "^3.3.2",
    "prettier-plugin-sh": "^0.14.0"
  },
  "pre-commit": [
    "format"
  ]
}
